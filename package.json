{
  "name": "issue-tracker",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b && chmod +x packages/cli/dist/index.js packages/agentd/dist/index.js && npm run build -w @issue-tracker/web",
    "test": "vitest run --config vitest.workspace.ts",
    "typecheck": "tsc -b --pretty false && npm run typecheck -w @issue-tracker/web",
    "lint": "eslint .",
    "dev": "tsx packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.0",
    "eslint": "^10.6.0",
    "prettier": "^3.9.4",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1",
    "vitest": "^4.1.9"
  }
}
